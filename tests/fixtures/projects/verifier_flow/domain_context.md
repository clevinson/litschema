# Domain context — verifier flow fixture

Synthetic agricultural field trials, written for the browser flow check in
`tests/browser_verify_flow.py`. The articles and numbers here are invented; they
exist to give the verifier something of every shape to render, not to describe
real experiments.
